@model IEnumerable<BUSS.Models.Destinasi>

@{
    ViewBag.Title = "Destinasi";
    ViewBag.SidebarMenu = "Destinasi";
}

<div class="page-inner">
    <div class="page-header">
        <h4 class="page-title">@ViewBag.Title</h4>
        <ul class="breadcrumbs">
            <li class="nav-home">
                <a href="/Admin">
                    <i class="flaticon-home"></i>
                </a>
            </li>
            <li class="separator">
                <i class="flaticon-right-arrow"></i>
            </li>
            <li class="nav-item">
                <a href="/Admin/@ViewBag.Title">@ViewBag.Title</a>
            </li>
        </ul>
    </div>
    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h3 class="card-title">Data @ViewBag.Title</h3>
                    <a href="/Admin/@ViewBag.Title/Create" class="btn btn-primary"><i class="fa fa-plus-circle"></i> Tambah Data</a>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table" id="example2">
                            <thead>
                                <tr>
                                    <th>No.</th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.Nama_Destinasi)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.Kota.Nama_Kota)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.Harga_Tiket)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.Rating)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.Jam_buka)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.Jam_tutup)
                                    </th>
                                    <th>Aksi</th>
                                </tr>
                            </thead>
                            <tbody>
                                @{ int rowCount = 1; }
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>@(rowCount++).</td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.Nama_Destinasi)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.Kota.Nama_Kota)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.Harga_Tiket)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.Rating)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.Jam_buka)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.Jam_tutup)
                                        </td>
                                        <td>
                                            <a href="@Url.Action("Edit", "Destinasi", new { id = item.ID_Destinasi })" class="btn btn-warning btn-sm mr-1">
                                                <i class="fa fa-edit"></i> Ubah
                                            </a>
                                            <a href="@Url.Action("Details", "Destinasi", new { id = item.ID_Destinasi })" class="btn btn-info btn-sm mr-1">
                                                <i class="fa fa-eye"></i> Detil
                                            </a>
                                            <form action="@Url.Action("Delete", "Destinasi")" method="post" class="d-inline deleteForm">
                                                @Html.AntiForgeryToken()
                                                <input type="hidden" name="id" value="@item.ID_Destinasi" />
                                                <button type="submit" class="btn btn-danger btn-sm"><i class="fa fa-trash"></i> Hapus</button>
                                            </form>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section scripts {
    <script>
        if ("@TempData["SuccessMessage"]" != "") {
            swal("Berhasil", "@TempData["SuccessMessage"]", {
                icon: "success",
                buttons: {
                    confirm: {
                        className: 'btn btn-success'
                    }
                },
            });
        }
    </script>
}